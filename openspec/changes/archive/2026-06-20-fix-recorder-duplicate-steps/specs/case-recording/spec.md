## 新增需求

### 需求:录制事件唯一性
每个用户在浏览器中的操作必须仅在录制结果中生成恰好一条步骤事件，禁止重复发射。

#### 场景:点击元素不重复
- **当** 用户在录制过程中点击一个页面元素
- **那么** 录制结果中仅包含一条 `{action: "click", selector: "..."}` 步骤

#### 场景:输入文本不重复
- **当** 用户在录制过程中在输入框中键入文本
- **那么** 录制结果中仅包含一条 `{action: "fill", selector: "...", value: "..."}` 步骤

#### 场景:页面导航不重复
- **当** 用户在录制过程中通过链接导航到新页面
- **那么** 录制结果中仅包含一条 `{action: "goto", url: "..."}` 步骤

#### 场景:连续多次操作不重复
- **当** 用户连续执行多次不同操作（如点击 → 输入 → 点击）
- **那么** 录制结果中每种操作各出现恰好一次，按操作顺序排列，无任何重复
